
Step 1:
//On laptop
docker build -t my-app:v1 .


//On GCP instance
//to install docker
sudo apt-get update
sudo apt-get install -y docker.io
sudo systemctl start docker
sudo systemctl enable docker
sudo usermod -aG docker $USER
//Already did this part 

docker build -t my-app:v1 . 
or docker build --network=host -t my-app:v1 . 

Step 2:
//On GCP
docker run -d -p 8080:8080 --name gcp-flask-app my-app:v1

Step 3:
//On Laptop
docker run -d -p 8080:8080 --name local-flask-app my-app:v1

Step 4:
curl http://localhost:8080

//On GCP
curl http://<External IP>:8080

http://<External IP>:8080
